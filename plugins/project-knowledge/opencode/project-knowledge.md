# Project Knowledge

For non-trivial work in a Git repository, load the `project-knowledge` Skill and resolve the current repository before relying on prior project decisions or implementation history. Use the Project Knowledge MCP tools as a read-only source, retrieve full entries only after focused search, and verify older knowledge against the current code.
