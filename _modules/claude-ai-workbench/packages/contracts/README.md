# @claude-ai-workbench/contracts

Claude AI Workbench 的公共数据契约和 TypeScript 类型，等价于传统 SDK 的公共头文件。

```ts
import type { AiProfile, AiProfileInput, ProfileTestResult } from "@claude-ai-workbench/contracts";
```

本包不包含运行时服务，也不接触 API Key。
